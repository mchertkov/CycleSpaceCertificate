# Notebook

Start Jupyter from the repository root:

```bash
jupyter notebook notebooks/CycleSpaceCertificates.ipynb
```

The notebook contains the full static and transient workflow. The transient certificate is computationally expensive because every bisection point solves all 92 signed cohesive boundary faces of the case39 reduction.
