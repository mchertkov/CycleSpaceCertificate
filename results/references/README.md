# Manuscript reference outputs

These files record the numerical values printed in the manuscript. They are regression targets, not a substitute for a fresh execution. Fresh runs are written to `../generated/`.
